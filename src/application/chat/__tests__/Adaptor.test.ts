import { MessageAdaptor } from '@application/chat/Adaptor';

describe('test', () => {
  beforeAll(() => {
    console.log(MessageAdaptor.name);
  });
  it('should ', function () {
    expect(true).toBeTruthy();
  });
});

export {};
